import { InputHTMLAttributes, forwardRef } from 'react';
import { clsx } from 'clsx';

interface InputProps extends InputHTMLAttributes { }

export const Input = forwardRef(
    ({ className, ...props }, ref) =& gt; {
    return (
      
    );
}
);

Input.displayName = 'Input';